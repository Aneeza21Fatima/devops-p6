pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Git checkout stage'
            }
        }

        stage('Build') {
            steps {
                echo 'Build stage'
            }
        }

        stage('Docker') {
            steps {
                echo 'Docker stage'
            }
        }
    }
}git 